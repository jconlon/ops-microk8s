apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vllm-tls
  namespace: kgateway-system
spec:
  secretName: vllm-tls
  dnsNames:
    - vllm.verticon.com
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
