apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: loki-tls
  namespace: kgateway-system
spec:
  secretName: loki-tls
  dnsNames:
    - loki.verticon.com
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
