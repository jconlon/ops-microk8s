apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls
  namespace: kgateway-system
spec:
  secretName: argocd-tls
  dnsNames:
    - argocd.verticon.com
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
