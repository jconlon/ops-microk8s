apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kafka-tls
  namespace: kgateway-system
spec:
  secretName: kafka-tls
  dnsNames:
    - kafka.verticon.com
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
