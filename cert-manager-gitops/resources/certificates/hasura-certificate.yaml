apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hasura-tls
  namespace: kgateway-system
spec:
  secretName: hasura-tls
  dnsNames:
    - hasura.verticon.com
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
