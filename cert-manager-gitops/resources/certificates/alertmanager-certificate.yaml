apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: alertmanager-tls
  namespace: kgateway-system
spec:
  secretName: alertmanager-tls
  dnsNames:
    - alertmanager.verticon.com
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
