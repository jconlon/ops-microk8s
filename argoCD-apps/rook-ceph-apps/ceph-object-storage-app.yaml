apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ceph-object-storage
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"  # Deploy after storageclasses
spec:
  project: default

  source:
    repoURL: https://github.com/jconlon/ops-microk8s.git
    targetRevision: main
    path: rook-ceph/object-storage

  destination:
    server: https://kubernetes.default.svc
    namespace: rook-ceph

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true

    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
