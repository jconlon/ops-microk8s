apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rssbridge
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jconlon/ops-microk8s
    targetRevision: HEAD
    path: rssbridge-gitops
  destination:
    server: https://kubernetes.default.svc
    namespace: rssbridge
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
