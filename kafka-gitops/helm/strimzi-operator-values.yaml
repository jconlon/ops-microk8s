# Strimzi Kafka Operator Helm Values

# Resource limits for the operator
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 512Mi

# Enable Prometheus monitoring
dashboards:
  enabled: true
